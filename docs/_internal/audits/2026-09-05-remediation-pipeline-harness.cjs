// Archived diagnostic: embedded inspected source, not future workspace code.
// Prints counterexamples; exit 0 is NOT a release pass. No real I/O is performed by the fixtures.

const vm=require('node:vm'),path=require('node:path'),{EventEmitter}=require('node:events');
const sources={"detail":"function toArr(val) {\n    if (!val) return [];\n    if (Array.isArray(val)) return val;\n    if (typeof val === 'object') return Object.values(val);\n    return [];\n}\n\nfunction parseDetailAnimals(doc) {\n    const list = toArr(doc && doc.animals).filter((a) => a && typeof a === 'object');\n    return list.length ? list : null;\n}\n\nfunction countCapturedHeads(animals) {\n    let sum = 0;\n    for (const a of animals) {\n        const c = Number(a && a.clusterCount);\n        if (a && a.__lodClusterAggregate && Number.isFinite(c) && c > 0) {\n            sum += Math.floor(c);\n        } else {\n            sum += 1;\n        }\n    }\n    return sum;\n}\n\nfunction hasUniqueIndividuals(animals) {\n    if (!Array.isArray(animals)) return false;\n    for (const a of animals) {\n        if (a && a.uniqueId != null && String(a.uniqueId).trim() !== '') return true;\n    }\n    return false;\n}\n\nfunction rememberDetailEntry(map, entry) {\n    const pid = entry.placeableId;\n    if (pid == null || !Number.isFinite(pid)) return;\n    const farm = Number(entry.ownerFarmId) || 0;\n    const key = `${pid}|${farm}`;\n    const prev = map.get(key);\n    if (!prev) {\n        map.set(key, entry);\n        return;\n    }\n    const prevN = countCapturedHeads(prev.animals);\n    const nextN = countCapturedHeads(entry.animals);\n    const prevU = hasUniqueIndividuals(prev.animals);\n    const nextU = hasUniqueIndividuals(entry.animals);\n    if (nextU && !prevU) {\n        map.set(key, entry);\n        return;\n    }\n    const prevAt = Number(prev.generatedAt) || 0;\n    const nextAt = Number(entry.generatedAt) || 0;\n    if (nextAt > 0 && prevAt > 0 && nextAt !== prevAt) {\n        if (nextAt >= prevAt) map.set(key, entry);\n        return;\n    }\n    if (nextU === prevU && nextN < prevN && nextU) {\n        map.set(key, entry);\n        return;\n    }\n    if (nextU === prevU && nextN > prevN) map.set(key, entry);\n}\n\nfunction lookupDetailEntry(map, hid, hf) {\n    const farm = Number(hf) || 0;\n    const exact = map.get(`${hid}|${farm}`);\n    if (exact) return exact;\n    if (farm !== 0) {\n        const unscoped = map.get(`${hid}|0`);\n        if (unscoped) return unscoped;\n    }\n    return null;\n}\n\nfunction husbandryAggregateHeadCount(h) {\n    const reported = Number(h.numOfAnimalsReported);\n    const prevCount = Number(h.animalCount);\n    let clusterSum = 0;\n    if (Array.isArray(h.clusters)) {\n        for (const c of h.clusters) {\n            const cc = Number(c && c.count);\n            if (Number.isFinite(cc) && cc > 0) clusterSum += cc;\n        }\n    }\n    return Math.max(\n        Number.isFinite(reported) && reported > 0 ? reported : 0,\n        Number.isFinite(prevCount) && prevCount > 0 ? prevCount : 0,\n        clusterSum\n    );\n}\n\nfunction shouldSkipIncompleteDetail(capturedHeads, aggregate, animals) {\n    if (!(aggregate > 0 && capturedHeads < aggregate)) return false;\n    // Cloned cluster averages are worse than a slightly short unique-id list.\n    if (hasUniqueIndividuals(animals) && capturedHeads / aggregate >= UNIQUE_CAPTURE_RATIO) {\n        return false;\n    }\n    return true;\n}\n\nfunction applyDetailBlockToHusbandry(h, block) {\n    const hf = Number(h.ownerFarmId ?? h.farmId ?? 0);\n    if (block.ownerFarmId && hf && block.ownerFarmId !== hf) {\n        return { husbandry: h, hydrated: false, heads: 0 };\n    }\n\n    const resolvedFarm = hf > 0 ? hf : block.ownerFarmId > 0 ? block.ownerFarmId : hf;\n    const ownerFarmId = resolvedFarm > 0 ? resolvedFarm : block.ownerFarmId || hf;\n    const animals = block.animals.map((a) => {\n        const row = {\n            ...a,\n            ownerFarmId: a.ownerFarmId ?? a.farmId ?? ownerFarmId,\n            farmId: a.farmId ?? a.ownerFarmId ?? ownerFarmId,\n        };\n        const grp = Number(a.count);\n        if (Number.isFinite(grp) && grp > 1) {\n            row.__lodClusterAggregate = true;\n            row.clusterCount = grp;\n        }\n        return row;\n    });\n\n    const capturedHeads = countCapturedHeads(animals);\n    const reported = Number(h.numOfAnimalsReported);\n    const prevCount = Number(h.animalCount);\n    const aggregate = husbandryAggregateHeadCount(h);\n\n    if (shouldSkipIncompleteDetail(capturedHeads, aggregate, animals)) {\n        return { husbandry: h, hydrated: false, heads: 0 };\n    }\n\n    return {\n        husbandry: {\n            ...h,\n            ownerFarmId,\n            farmId: ownerFarmId,\n            animals,\n            lod: 'full',\n            animalCount: Number.isFinite(reported) && reported >= 0 ? reported : capturedHeads,\n            numOfAnimalsReported: Number.isFinite(reported) && reported >= 0 ? reported : capturedHeads,\n            __detailHydrated: true,\n            __detailCapturedHeads: capturedHeads,\n        },\n        hydrated: true,\n        heads: capturedHeads,\n    };\n}\nfunction makeCacheEntry(st, doc, animals) {\n    return {\n        mtimeMs: st.mtimeMs,\n        size: st.size,\n        animals,\n        placeableId: doc.placeableId != null ? Number(doc.placeableId) : null,\n        ownerFarmId: doc.ownerFarmId != null ? Number(doc.ownerFarmId) : 0,\n        penId: doc.penId,\n    };\n}","fingerprint":"async function getSavegameXmlFingerprint(srv, saveSlot) {\n    const savegameDir = await getSavegamePathAsync(srv, saveSlot);\n    if (!savegameDir) return null;\n    let maxMtime = 0;\n    let totalSize = 0;\n    for (const f of SAVEGAME_XML_FILES.concat(OPTIONAL_SAVEGAME_XML_FILES)) {\n        const p = path.join(savegameDir, f);\n        try {\n            const st = await fs.stat(p);\n            if (st.mtimeMs > maxMtime) maxMtime = st.mtimeMs;\n            totalSize += st.size;\n        } catch (_) {\n            /* optional file */\n        }\n    }\n    if (maxMtime === 0) return null;\n    return `${maxMtime}:${totalSize}`;\n}","match":"    const takeClosest = (list, luaV) => {\n        if (!list || list.length === 0) return null;\n        let bestIdx = 0;\n        if (luaV.position) {\n            let best = Infinity;\n            for (let i = 0; i < list.length; i++) {\n                const xp = list[i].position;\n                if (!xp) continue;\n                const d = Math.hypot(\n                    (luaV.position.x ?? 0) - (xp.x ?? 0),\n                    (luaV.position.z ?? 0) - (xp.z ?? 0)\n                );\n                if (d < best) { best = d; bestIdx = i; }\n            }\n        }\n        return list.splice(bestIdx, 1)[0];\n    };\n\n    const removeFromBuckets = (xmlV) => {\n        const k = vehicleConfigKey(xmlV);\n        if (k) {\n            const farmBucket = xmlByKey.get(k);\n            if (farmBucket) {\n                const idx = farmBucket.indexOf(xmlV);\n                if (idx >= 0) farmBucket.splice(idx, 1);\n            }\n        }\n        const cfg = vehicleConfigBasename(xmlV);\n        if (cfg) {\n            const cfgBucket = xmlByCfg.get(cfg);\n            if (cfgBucket) {\n                const idx = cfgBucket.indexOf(xmlV);\n                if (idx >= 0) cfgBucket.splice(idx, 1);\n            }\n        }\n    };\n\n    const takeXmlMatch = (luaV) => {\n        const uid = vehicleUniqueId(luaV);\n        if (uid && xmlByUid.has(uid)) {\n            const xmlV = xmlByUid.get(uid);\n            xmlByUid.delete(uid);\n            removeFromBuckets(xmlV);\n            return xmlV;\n        }\n        if (luaV?.id != null) {\n            const idStr = String(luaV.id);\n            if (xmlByUid.has(idStr)) {\n                const xmlV = xmlByUid.get(idStr);\n                xmlByUid.delete(idStr);\n                removeFromBuckets(xmlV);\n                return xmlV;\n            }\n            for (const [xuid, xmlV] of xmlByUid.entries()) {\n                if (xuid === idStr) {\n                    xmlByUid.delete(xuid);\n                    removeFromBuckets(xmlV);\n                    return xmlV;\n                }\n            }\n        }\n\n        const k = vehicleConfigKey(luaV);\n        const keyed = k ? xmlByKey.get(k) : null;\n        if (keyed && keyed.length > 0) {\n            return takeClosest(keyed, luaV);\n        }\nreturn null;};","http":"function httpGetBuffer(urlStr, timeoutMs = 25000, hopsLeft = HTTP_FEED_MAX_HOPS, deadlineAt = 0) {\n    const deadline = deadlineAt || Date.now() + HTTP_FEED_DEADLINE_MS;\n    return new Promise((resolve, reject) => {\n        if (Date.now() > deadline) {\n            reject(new Error('HTTP feed total deadline exceeded'));\n            return;\n        }\n        let parsed;\n        try {\n            parsed = new URL(urlStr);\n        } catch (e) {\n            reject(e);\n            return;\n        }\n        if (parsed.protocol !== 'http:' && parsed.protocol !== 'https:') {\n            reject(new Error('HTTP feed protocol not allowed'));\n            return;\n        }\n        const remainMs = Math.max(1000, Math.min(timeoutMs, deadline - Date.now()));\n        const lib = parsed.protocol === 'https:' ? https : http;\n        const req = lib.get(\n            urlStr,\n            {\n                timeout: remainMs,\n                headers: { Accept: 'application/xml,text/xml,*/*' },\n            },\n            (res) => {\n                const status = res.statusCode || 0;\n                if (status >= 300 && status < 400 && res.headers.location) {\n                    res.resume();\n                    if (hopsLeft <= 0) {\n                        reject(new Error('HTTP feed redirect limit'));\n                        return;\n                    }\n                    let next;\n                    try {\n                        next = new URL(res.headers.location, parsed).toString();\n                    } catch (e) {\n                        reject(e);\n                        return;\n                    }\n                    httpGetBuffer(next, timeoutMs, hopsLeft - 1, deadline).then(resolve, reject);\n                    return;\n                }\n                const chunks = [];\n                let bytes = 0;\n                res.on('data', (c) => {\n                    bytes += c.length;\n                    if (bytes > HTTP_FEED_MAX_BYTES) {\n                        req.destroy();\n                        reject(new Error('HTTP feed response too large'));\n                        return;\n                    }\n                    chunks.push(c);\n                });\n                res.on('end', () => {\n                    const buf = Buffer.concat(chunks);\n                    resolve({ status, buf, contentType: String(res.headers['content-type'] || '') });\n                });\n            }\n        );\n        req.on('timeout', () => {\n            req.destroy();\n            reject(new Error('HTTP feed request timed out'));\n        });\n        req.on('error', reject);\n    });\n}"};
function run(code,g={}){const module={exports:{}};vm.runInNewContext(code,{module,exports:module.exports,console,URL,Buffer,Date,path,...g});return module.exports;}
(async()=>{
const out=[];
const detail=run(sources.detail+'\nmodule.exports={applyDetailBlockToHusbandry,makeCacheEntry};',{UNIQUE_CAPTURE_RATIO:.8});
const applied=detail.applyDetailBlockToHusbandry({id:7,ownerFarmId:1,animalCount:0,numOfAnimalsReported:0,clusters:[]},{ownerFarmId:1,animals:[{uniqueId:'old1'},{uniqueId:'old2'}]});
out.push({case:'authoritative zero pen rejects retained detail',actual:{reported:applied.husbandry.numOfAnimalsReported,rows:applied.husbandry.animals.length,hydrated:applied.hydrated},expected:'zero rows, not hydrated',pass:applied.husbandry.animals.length===0});
const entry=detail.makeCacheEntry({mtimeMs:1,size:2},{placeableId:7,ownerFarmId:1,generatedAt:123},[]);
out.push({case:'detail generation survives cache construction',actual:entry.generatedAt??null,expected:123,pass:entry.generatedAt===123});
let older=1000;
const fp=run(sources.fingerprint+'\nmodule.exports={getSavegameXmlFingerprint};',{getSavegamePathAsync:async()=> 'C:/audit',SAVEGAME_XML_FILES:['career.xml','fields.xml'],OPTIONAL_SAVEGAME_XML_FILES:[],fs:{stat:async p=>({mtimeMs:p.endsWith('career.xml')?9999:older,size:100})}});
const first=await fp.getSavegameXmlFingerprint({},'savegame1');older=2000;const second=await fp.getSavegameXmlFingerprint({},'savegame1');
out.push({case:'nonmaximum XML mtime change invalidates collection',before:first,after:second,expected:'different',pass:first!==second});
const x={uid:'u1',config:'tractor',ownerFarmId:1};const uid=new Map([['u1',x]]),key=new Map([['tractor|1',[x]]]),cfg=new Map([['tractor',[x]]]);
const matcher=run(sources.match+'\nmodule.exports={takeXmlMatch};',{xmlByUid:uid,xmlByKey:key,xmlByCfg:cfg,vehicleUniqueId:v=>v.uid||'',vehicleConfigKey:v=>v.config+'|'+v.ownerFarmId,vehicleConfigBasename:v=>v.config});
const one=matcher.takeXmlMatch({config:'tractor',ownerFarmId:1}),two=matcher.takeXmlMatch({uid:'u1',config:'tractor',ownerFarmId:1});
out.push({case:'XML row consumed once across matching indices',firstMatched:one===x,secondMatchedSame:two===x,expected:'second cannot consume the same row',pass:one===x&&two!==x});
let now=0,response;const req=new EventEmitter();req.destroy=()=>{};const http={get:(_url,_opts,cb)=>{response=new EventEmitter();response.statusCode=200;response.headers={};queueMicrotask(()=>cb(response));return req}};
const mod=run(sources.http+'\nmodule.exports={httpGetBuffer};',{Date:{now:()=>now},HTTP_FEED_MAX_HOPS:5,HTTP_FEED_DEADLINE_MS:45000,HTTP_FEED_MAX_BYTES:8*1024*1024,http,https:http});
let settled=false;const promise=mod.httpGetBuffer('http://synthetic.invalid/feed').then(x=>{settled=true;return x},e=>{settled=true;throw e});
await Promise.resolve();for(now of [20000,40000,60000]){response.emit('data',Buffer.from('x'));await Promise.resolve();}
const overduePending=!settled;response.emit('end');const value=await promise;
out.push({case:'HTTP absolute 45-second deadline terminates a trickle response',pendingAt60000ms:overduePending,eventualStatus:value.status,expected:'rejected by total deadline',pass:false});
console.log(JSON.stringify({fixtureScope:'isolated production snippets with mocked I/O and clocks',results:out},null,2));
})().catch(e=>{console.error(e);process.exitCode=1});

