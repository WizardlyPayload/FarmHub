// Archived diagnostic: embedded inspected source, not future workspace code.
// Prints counterexamples; exit 0 is NOT a release pass. No real I/O is performed by the fixtures.

const vm=require('node:vm'),path=require('node:path');
const ts=require('./NEW APP/node_modules/typescript');
const sources={"policy":"'use strict';\r\n\r\n/**\r\n * Classic vs Realistic Farming (RF) runtime identity.\r\n * Packaged RF stamps extraMetadata.productLine = rf; Classic has no productLine.\r\n * Ports: Classic 8766, RF 8768, unpackaged FARMDASH_DEV 8767.\r\n */\r\n\r\nconst fs = require('fs');\r\nconst os = require('os');\r\nconst path = require('path');\r\n\r\nconst CLASSIC_PORT = 8766;\r\nconst RF_PORT = 8768;\r\nconst DEV_PORT = 8767;\r\n\r\nfunction resolveProductLine(opts) {\r\n    const o = opts || {};\r\n    const env = String(o.envProductLine || process.env.FARMDASH_PRODUCT_LINE || '')\r\n        .trim()\r\n        .toLowerCase();\r\n    if (env === 'rf' || env === 'realistic-farming') return 'rf';\r\n    let pkg = o.pkg;\r\n    if (!pkg) {\r\n        try {\r\n            pkg = require('./package.json');\r\n        } catch (_) {\r\n            pkg = {};\r\n        }\r\n    }\r\n    const line = String(pkg.productLine || '').trim().toLowerCase();\r\n    if (line === 'rf' || line === 'realistic-farming') return 'rf';\r\n    return 'classic';\r\n}\r\n\r\nfunction resolvePort(opts) {\r\n    const o = opts || {};\r\n    const raw = o.envPort != null ? o.envPort : process.env.FARMDASH_PORT;\r\n    if (raw != null && String(raw).trim() !== '') {\r\n        const n = parseInt(String(raw).trim(), 10);\r\n        if (Number.isFinite(n) && n >= 1024 && n <= 65535) return n;\r\n    }\r\n    if (o.isDev) return DEV_PORT;\r\n    return o.productLine === 'rf' ? RF_PORT : CLASSIC_PORT;\r\n}\r\n\r\nfunction profileDirName(productLine, isDev) {\r\n    if (isDev) return productLine === 'rf' ? 'fs25-farm-dashboard-rf-dev' : 'fs25-farm-dashboard-dev';\r\n    return productLine === 'rf' ? 'fs25-farm-dashboard-rf' : 'fs25-farm-dashboard';\r\n}\r\n\r\nfunction appUserModelId(productLine) {\r\n    return productLine === 'rf' ? 'com.farmdashboard.rf' : 'com.farmdashboard.app';\r\n}\r\n\r\nfunction otherEditionPort(productLine) {\r\n    return productLine === 'rf' ? CLASSIC_PORT : RF_PORT;\r\n}\r\n\r\nfunction roamingAppData() {\r\n    return process.env.APPDATA || path.join(os.homedir(), 'AppData', 'Roaming');\r\n}\r\n\r\nfunction localAppData() {\r\n    return process.env.LOCALAPPDATA || path.join(os.homedir(), 'AppData', 'Local');\r\n}\r\n\r\nfunction siblingClassicUserDataDir() {\r\n    return path.join(roamingAppData(), 'fs25-farm-dashboard');\r\n}\r\n\r\nfunction uninstallProfileNames(edition) {\r\n    const ed = String(edition || 'classic').trim().toLowerCase();\r\n    if (ed === 'rf') {\r\n        return {\r\n            roaming: ['fs25-farm-dashboard-rf'],\r\n            local: [\r\n                'fs25-farm-dashboard-rf',\r\n                'fs25-farm-dashboard-rf-updater',\r\n                'com.farmdashboard.rf-updater',\r\n            ],\r\n        };\r\n    }\r\n    return {\r\n        roaming: ['fs25-farm-dashboard', 'FS25 Farm Dashboard', 'com.farmdashboard.app'],\r\n        local: [\r\n            'fs25-farm-dashboard',\r\n            'fs25-farm-dashboard-updater',\r\n            'com.farmdashboard.app-updater',\r\n        ],\r\n    };\r\n}\r\n\r\n/**\r\n * Override userData / sessionData before Store and requestSingleInstanceLock.\r\n * No-op when Electron was started with --user-data-dir (dev launcher).\r\n */\r\nfunction applySessionPaths(electronApp, opts) {\r\n    const o = opts || {};\r\n    if (!electronApp || typeof electronApp.setPath !== 'function') return null;\r\n    try {\r\n        if (electronApp.commandLine && electronApp.commandLine.hasSwitch('user-data-dir')) {\r\n            return electronApp.getPath('userData');\r\n        }\r\n    } catch (_) {\r\n        /* ignore */\r\n    }\r\n    const name = profileDirName(o.productLine, !!o.isDev);\r\n    const userData = o.isDev ? path.join(localAppData(), name) : path.join(roamingAppData(), name);\r\n    const sessionData = path.join(localAppData(), name);\r\n    try {\r\n        fs.mkdirSync(userData, { recursive: true });\r\n    } catch (_) {\r\n        /* ignore */\r\n    }\r\n    try {\r\n        fs.mkdirSync(sessionData, { recursive: true });\r\n    } catch (_) {\r\n        /* ignore */\r\n    }\r\n    electronApp.setPath('userData', userData);\r\n    try {\r\n        electronApp.setPath('sessionData', sessionData);\r\n    } catch (_) {\r\n        /* older Electron */\r\n    }\r\n    try {\r\n        if (typeof electronApp.setAppUserModelId === 'function') {\r\n            electronApp.setAppUserModelId(appUserModelId(o.productLine));\r\n        }\r\n    } catch (_) {\r\n        /* ignore */\r\n    }\r\n    return userData;\r\n}\r\n\r\n/**\r\n * Copy Classic save list into RF without secrets or LAN exposure.\r\n * @returns {object|null} sanitized config or null if nothing to import\r\n */\r\nfunction sanitizeClassicConfigForRf(config) {\r\n    if (!config || typeof config !== 'object') return null;\r\n    const serversIn = Array.isArray(config.servers) ? config.servers : [];\r\n    const servers = [];\r\n    for (const s of serversIn) {\r\n        if (!s || typeof s !== 'object') continue;\r\n        const id = String(s.id || '').trim();\r\n        if (!id) continue;\r\n        const mode = s.mode === 'ftp' ? 'ftp' : 'local';\r\n        const row = {\r\n            id,\r\n            name: String(s.name || id),\r\n            mode,\r\n            localPath: s.localPath != null ? String(s.localPath) : undefined,\r\n            localSubFolder: s.localSubFolder != null ? String(s.localSubFolder) : undefined,\r\n            ftpHost: s.ftpHost != null ? String(s.ftpHost) : undefined,\r\n            ftpPort: s.ftpPort,\r\n            ftpUser: s.ftpUser != null ? String(s.ftpUser) : undefined,\r\n            ftpBasePath: s.ftpBasePath != null ? String(s.ftpBasePath) : undefined,\r\n            httpFeedHost: s.httpFeedHost != null ? String(s.httpFeedHost) : undefined,\r\n            httpFeedPort: s.httpFeedPort,\r\n        };\r\n        servers.push(row);\r\n    }\r\n    if (!servers.length) return null;\r\n    const out = { isConfigured: true, servers };\r\n    if (config.ftpPolling && typeof config.ftpPolling === 'object') {\r\n        out.ftpPolling = {\r\n            initialDelaySeconds: config.ftpPolling.initialDelaySeconds,\r\n            intervalMinutes: config.ftpPolling.intervalMinutes,\r\n            scheduleMode: config.ftpPolling.scheduleMode,\r\n        };\r\n    }\r\n    return out;\r\n}\r\n\r\nfunction readElectronStoreConfig(storeJsonPath) {\r\n    try {\r\n        const raw = fs.readFileSync(storeJsonPath, 'utf8');\r\n        const parsed = JSON.parse(raw);\r\n        if (parsed && typeof parsed === 'object' && parsed.config && typeof parsed.config === 'object') {\r\n            return parsed.config;\r\n        }\r\n        return null;\r\n    } catch (_) {\r\n        return null;\r\n    }\r\n}\r\n\r\n/**\r\n * Trusted desktop IPC: packaged/setup file documents, or exact loopback dashboard origin.\r\n * Does not accept LAN IPs or other ports.\r\n */\r\nfunction isTrustedDashboardIpcUrl(rawUrl, opts) {\r\n    const o = opts || {};\r\n    const port = String(o.port != null ? o.port : '');\r\n    if (!rawUrl || !port) return false;\r\n    let u;\r\n    try {\r\n        u = new URL(String(rawUrl));\r\n    } catch (_) {\r\n        return false;\r\n    }\r\n    if (u.protocol === 'file:') {\r\n        const p = decodeURIComponent(u.pathname || '').replace(/\\\\/g, '/').toLowerCase();\r\n        return (\r\n            p.endsWith('/setup.html') ||\r\n            p.endsWith('/index.html') ||\r\n            /\\/ui-v2\\/setup\\.html$/.test(p) ||\r\n            /\\/ui-v2\\/index\\.html$/.test(p)\r\n        );\r\n    }\r\n    if (u.protocol !== 'http:' && u.protocol !== 'https:') return false;\r\n    const host = String(u.hostname || '').toLowerCase();\r\n    if (host !== '127.0.0.1' && host !== 'localhost' && host !== '::1') return false;\r\n    const urlPort = String(u.port || (u.protocol === 'https:' ? '443' : '80'));\r\n    return urlPort === port;\r\n}\r\n\r\nfunction windowTitle(opts) {\r\n    const o = opts || {};\r\n    const rf = o.productLine === 'rf';\r\n    const base = rf ? 'FS25 Farm Dashboard RF' : 'FS25 Farm Dashboard';\r\n    if (o.isDev) return `${base} (dev :${o.port})`;\r\n    return base;\r\n}\r\n\r\nfunction portInUseMessage(opts) {\r\n    const o = opts || {};\r\n    const edition = o.productLine === 'rf' ? 'Farm Dashboard RF' : 'Farm Dashboard (Classic)';\r\n    const peer =\r\n        o.productLine === 'rf'\r\n            ? 'Classic uses port 8766; this RF copy uses 8768.'\r\n            : 'RF uses port 8768; this Classic copy uses 8766.';\r\n    return (\r\n        `Port ${o.port} is already in use — ${edition} cannot start its API.\\n\\n` +\r\n        `Close another window of this same edition (not the other product line). ${peer}\\n` +\r\n        'Then restart this app. Do not end unrelated Electron or Farming Simulator tasks.'\r\n    );\r\n}\r\n\r\nmodule.exports = {\r\n    CLASSIC_PORT,\r\n    RF_PORT,\r\n    DEV_PORT,\r\n    resolveProductLine,\r\n    resolvePort,\r\n    profileDirName,\r\n    appUserModelId,\r\n    otherEditionPort,\r\n    siblingClassicUserDataDir,\r\n    uninstallProfileNames,\r\n    applySessionPaths,\r\n    sanitizeClassicConfigForRf,\r\n    readElectronStoreConfig,\r\n    isTrustedDashboardIpcUrl,\r\n    windowTitle,\r\n    portInUseMessage,\r\n};\r","hook":"import { useEffect, useMemo, useRef, useState } from \"preact/hooks\";\r\nimport { loadPenDetail } from \"@/lib/pen-detail\";\r\nimport {\r\n  collectPensNeedingDetailHydration,\r\n  mergeAnimalsWithPenHydration,\r\n  penDetailAnimalsLookIndividual,\r\n  penDetailAnimalsToLivestockRows,\r\n} from \"@/lib/livestock-hydrate\";\r\nimport { normalizeLivestockAnimals } from \"@/lib/livestock-normalize\";\r\nimport type { LivestockAnimal, LodGlobalState } from \"@/lib/livestock-types\";\r\n\r\nexport function useHydratedLivestockAnimals(opts: {\r\n  animalsData: unknown;\r\n  activeFarmId: number | null;\r\n  activeServerId: string | null;\r\n  /** When set, only that pen is detail-hydrated (livestock panel). */\r\n  husbandryId?: string | number | null;\r\n}): {\r\n  flattenedAnimals: LivestockAnimal[];\r\n  animals: LivestockAnimal[];\r\n  lodState: LodGlobalState;\r\n} {\r\n  const { animalsData, activeFarmId, activeServerId, husbandryId } = opts;\r\n\r\n  const { animals: flattenedAnimals, lodState } = useMemo(\r\n    () => normalizeLivestockAnimals(animalsData, activeFarmId),\r\n    [animalsData, activeFarmId]\r\n  );\r\n\r\n  const [hydratedByPen, setHydratedByPen] = useState<Record<string, LivestockAnimal[]>>({});\r\n\r\n  const allAnimals = useMemo(\r\n    () => mergeAnimalsWithPenHydration(flattenedAnimals, hydratedByPen),\r\n    [flattenedAnimals, hydratedByPen]\r\n  );\r\n\r\n  const pensHydrationKey = useMemo(() => {\r\n    const need = collectPensNeedingDetailHydration(flattenedAnimals);\r\n    const scoped =\r\n      husbandryId == null || husbandryId === \"\"\r\n        ? need\r\n        : need.filter((id) => id === String(husbandryId));\r\n    return scoped.slice().sort().join(\",\");\r\n  }, [flattenedAnimals, husbandryId]);\r\n\r\n  const flattenedRef = useRef(flattenedAnimals);\r\n  flattenedRef.current = flattenedAnimals;\r\n  const hydratedRef = useRef(hydratedByPen);\r\n  hydratedRef.current = hydratedByPen;\r\n\r\n  useEffect(() => {\r\n    setHydratedByPen({});\r\n  }, [activeServerId, activeFarmId]);\r\n\r\n  useEffect(() => {\r\n    const pensNeedingHydration = pensHydrationKey ? pensHydrationKey.split(\",\") : [];\r\n    if (pensNeedingHydration.length === 0) return;\r\n    let cancelled = false;\r\n    const farmId = Number(activeFarmId ?? 1);\r\n    const run = async () => {\r\n      let cursor = 0;\r\n      const workers = Math.min(4, pensNeedingHydration.length);\r\n      const worker = async () => {\r\n        while (!cancelled) {\r\n          const i = cursor;\r\n          cursor += 1;\r\n          if (i >= pensNeedingHydration.length) return;\r\n          const pid = pensNeedingHydration[i];\r\n          if (hydratedRef.current[pid]?.length) continue;\r\n          const env = await loadPenDetail(pid, { serverId: activeServerId });\r\n          const list = env?.detail?.animals;\r\n          if (cancelled || !list?.length || !penDetailAnimalsLookIndividual(list)) continue;\r\n          const template = flattenedRef.current.find(\r\n            (a) => String(a.husbandryId ?? a.huId ?? \"\") === pid\r\n          );\r\n          const rows = penDetailAnimalsToLivestockRows(pid, list, template, farmId);\r\n          if (!cancelled && rows.length) {\r\n            setHydratedByPen((prev) => (prev[pid] ? prev : { ...prev, [pid]: rows }));\r\n          }\r\n        }\r\n      };\r\n      await Promise.all(Array.from({ length: workers }, () => worker()));\r\n    };\r\n    void run();\r\n    return () => {\r\n      cancelled = true;\r\n    };\r\n  }, [pensHydrationKey, activeServerId, activeFarmId]);\r\n\r\n  return { flattenedAnimals, animals: allAnimals, lodState };\r\n}\r","config":"// FS25 FarmDashboard | setupConfigMerge.cjs\n// Persist-safe merge for Settings / first-run config (keeps FTP secrets; refuses empty wipe).\n\nfunction mergeServersPreserveSecrets(prevServers, incomingServers) {\n  if (!Array.isArray(incomingServers)) return [];\n  const prevById = new Map((prevServers || []).map((s) => [String(s.id), s]));\n  return incomingServers.map((inc) => {\n    if (!inc || typeof inc !== \"object\") return inc;\n    const prev = prevById.get(String(inc.id));\n    const out = { ...inc };\n    delete out.ftpPassSet;\n    delete out.httpFeedCodeSet;\n    if (inc.mode === \"ftp\" && prev) {\n      const emptyPass = inc.ftpPass == null || String(inc.ftpPass).trim() === \"\";\n      if (emptyPass && prev.ftpPass) out.ftpPass = prev.ftpPass;\n    }\n    const emptyCode = inc.httpFeedCode == null || String(inc.httpFeedCode).trim() === \"\";\n    if (emptyCode && prev && prev.httpFeedCode) out.httpFeedCode = prev.httpFeedCode;\n    return out;\n  });\n}\n\n/**\n * Empty `servers: []` from a race (Save before settings finished loading) must not\n * erase an existing save list on disk.\n */\nfunction resolveServersForSave(prevServers, incomingServers) {\n  const prev = Array.isArray(prevServers) ? prevServers : [];\n  if (!Array.isArray(incomingServers)) {\n    return { servers: prev.slice(), keptExisting: prev.length > 0 };\n  }\n  if (incomingServers.length === 0 && prev.length > 0) {\n    return { servers: prev.slice(), keptExisting: true };\n  }\n  return {\n    servers: mergeServersPreserveSecrets(prev, incomingServers),\n    keptExisting: false,\n  };\n}\n\nfunction secretRevision(v) {\n  const s = String(v || \"\");\n  if (!s) return \"0\";\n  let h = 2166136261;\n  for (let i = 0; i < s.length; i++) {\n    h ^= s.charCodeAt(i);\n    h = Math.imul(h, 16777619);\n  }\n  return `${s.length}:${(h >>> 0).toString(16)}`;\n}\n\nfunction serverListSignature(servers) {\n  return (Array.isArray(servers) ? servers : [])\n    .map((s) => {\n      if (!s || typeof s !== \"object\") return \"\";\n      return [\n        s.id,\n        s.mode,\n        s.localSubFolder || \"\",\n        s.localPath || \"\",\n        s.ftpHost || \"\",\n        s.ftpPort || \"\",\n        s.ftpUser || \"\",\n        s.ftpBasePath || \"\",\n        secretRevision(s.ftpPass),\n        s.httpFeedHost || \"\",\n        s.httpFeedPort || \"\",\n        secretRevision(s.httpFeedCode),\n        s.name || \"\",\n      ].join(\"|\");\n    })\n    .join(\";\");\n}\n\nfunction ftpPollingEqual(a, b) {\n  const x = a && typeof a === \"object\" ? a : {};\n  const y = b && typeof b === \"object\" ? b : {};\n  return (\n    Number(x.initialDelaySeconds ?? 0) === Number(y.initialDelaySeconds ?? 0) &&\n    Number(x.intervalMinutes ?? 5) === Number(y.intervalMinutes ?? 5) &&\n    String(x.scheduleMode || \"sync\") === String(y.scheduleMode || \"sync\")\n  );\n}\n\nfunction configNeedsServerReboot(prevConfig, nextConfig) {\n  const prev = prevConfig && typeof prevConfig === \"object\" ? prevConfig : {};\n  const next = nextConfig && typeof nextConfig === \"object\" ? nextConfig : {};\n  if (serverListSignature(prev.servers) !== serverListSignature(next.servers)) return true;\n  if (!ftpPollingEqual(prev.ftpPolling, next.ftpPolling)) return true;\n  return false;\n}\n\n/** Union by id: keep local/dev rows, then append installed-only ids (dev:new-ui --sync-config). */\nfunction unionServersById(installedServers, localServers) {\n  const inst = Array.isArray(installedServers) ? installedServers : [];\n  const local = Array.isArray(localServers) ? localServers : [];\n  if (local.length === 0) return inst.slice();\n  const seen = new Set();\n  const out = [];\n  for (const s of local) {\n    if (!s || s.id == null) continue;\n    const id = String(s.id);\n    if (seen.has(id)) continue;\n    seen.add(id);\n    out.push(s);\n  }\n  for (const s of inst) {\n    if (!s || s.id == null) continue;\n    const id = String(s.id);\n    if (seen.has(id)) continue;\n    seen.add(id);\n    out.push(s);\n  }\n  return out;\n}\n\nmodule.exports = {\n  mergeServersPreserveSecrets,\n  resolveServersForSave,\n  serverListSignature,\n  ftpPollingEqual,\n  configNeedsServerReboot,\n  unionServersById,\n};\n","origin":"function getRequestOrigin(req) {\n    const o = req.headers && (req.headers.origin || req.headers.referer || '');\n    if (!o) return '';\n    try {\n        return new URL(String(o)).origin;\n    } catch (_) { return ''; }\n}\nfunction isAllowedSameOrigin(originStr, req) {\n    if (!originStr) return false;\n    try {\n        const u = new URL(originStr);\n        const host = String(u.hostname || '').toLowerCase();\n        const port = String(u.port || (u.protocol === 'https:' ? 443 : 80));\n        if (port !== String(PORT)) return false;\n        // Loopback or this machine's own NIC IPs only — not an arbitrary host on the dashboard port.\n        if (isLocalServerHost(host)) return true;\n        // LAN tablet hitting <hostIp>:PORT with a matching Host header (still must be one of our IPs).\n        const hostHeader = String((req.headers && req.headers.host) || '').split(':')[0].toLowerCase();\n        if (hostHeader && host === hostHeader && isLocalServerHost(hostHeader)) return true;\n    } catch (_) { /* ignore */ }\n    return false;\n}\nfunction enforceWriteOriginAndToken(req, res) {\n    const origin = getRequestOrigin(req);\n    // Browser pages always send Origin/Referer. Reject hostile origins even on loopback.\n    // Native/curl with no Origin still allowed from this machine.\n    if (origin) {\n        if (!isAllowedSameOrigin(origin, req) && !corsOriginAllowedPure(origin, {\n            port: PORT,\n            marketingHosts: MARKETING_SITE_HOSTS,\n            localIps: getCachedLocalInterfaceIps(),\n        })) {\n            return { code: 403, body: { ok: false, error: 'origin not allowed' } };\n        }\n    } else if (!isLocalhostSocket(req) && !isRequestFromThisMachine(req)) {\n        return { code: 403, body: { ok: false, error: 'origin not allowed' } };\n    }\n    if (isLocalhostSocket(req)) return null;\n    // When LAN access is enabled the API binds beyond localhost — require setup token for writes.\n    // With LAN off, only local processes reach the port; same-origin check above still applies for browsers.\n    const lanOn = !!getLanSecurityFromStore().lanAccessEnabled;\n    if (!lanOn) return null;\n    ensureSetupWriteToken();\n    const expected = String(store.get('farmdashSetupWriteToken') || '');\n    const got = String(\n        (req.headers && (req.headers['x-farmdash-token'] || req.headers['x-setup-token'])) || ''\n    ).trim();\n    if (!expected || got !== expected) {\n        return { code: 403, body: { ok: false, error: 'invalid token' } };\n    }\n    return null;\n}\nfunction webSocketOriginAllowed(req) {\n    const origin = getRequestOrigin(req);\n    if (!origin) {\n        // Non-browser clients (no Origin). Allow only this machine.\n        return isLoopbackIp(requestRemoteAddress(req)) || isRequestFromThisMachine(req);\n    }\n    return corsOriginAllowedPure(origin, {\n        port: PORT,\n        marketingHosts: MARKETING_SITE_HOSTS,\n        localIps: getCachedLocalInterfaceIps(),\n    }) || isAllowedSameOrigin(origin, req);\n}","download":"async function safeDownload(client, remotePath, localTmp, localFinal, options = {}) {\n    const maxAttempts = Math.max(1, parseInt(options.maxAttempts, 10) || 1);\n    const retryDelayMs = Math.max(0, parseInt(options.retryDelayMs, 10) || 300);\n    let lastErr = '';\n    for (let attempt = 1; attempt <= maxAttempts; attempt++) {\n        try {\n            try {\n                await fs.promises.unlink(localTmp);\n            } catch (e) {\n                if (e && e.code !== 'ENOENT') {\n                    /* ignore transient unlink issues like old behavior */\n                }\n            }\n            await client.downloadTo(localTmp, remotePath);\n            let size = 0;\n            try {\n                size = (await fs.promises.stat(localTmp)).size;\n            } catch (e) {\n                lastErr = e && e.message ? String(e.message) : String(e);\n                if (attempt < maxAttempts) {\n                    await new Promise((r) => setTimeout(r, retryDelayMs * attempt));\n                }\n                continue;\n            }\n            if (size > 0) {\n                try {\n                    await fs.promises.unlink(localFinal);\n                } catch (e) {\n                    if (e && e.code !== 'ENOENT') {\n                        /* ignore */\n                    }\n                }\n                await fs.promises.rename(localTmp, localFinal);\n                return true;\n            }\n            lastErr = 'empty or zero-byte after download';\n        } catch (e) {\n            lastErr = e && e.message ? String(e.message) : String(e);\n        }\n        if (attempt < maxAttempts) {\n            await new Promise((r) => setTimeout(r, retryDelayMs * attempt));\n        }\n    }\n    if (maxAttempts > 1 && lastErr && options.logFailures) {\n        console.warn(`[FTP] ${path.basename(localFinal)}: failed after ${maxAttempts} tries — ${lastErr.slice(0, 120)}`);\n    }\n    return false;\n}","ftp":"async function syncFtpDetailsCache(client, srv, slotRemote, userDataPath) {\n    const remoteDir = `${slotRemote}/details`.replace(/\\\\/g, '/');\n    const localRoot = path.join(userDataPath, 'ftpDetailsCache', String(srv.id));\n    const st = serverStates[srv.id];\n    const folderName =\n        (st && st.lastSaveSlot) ||\n        srv.localSubFolder ||\n        'savegame1';\n    const localDir = path.join(localRoot, folderName, 'details');\n    try {\n        await fs.promises.mkdir(localDir, { recursive: true });\n    } catch (_) {\n        return 0;\n    }\n    let list;\n    try {\n        list = await client.list(remoteDir);\n    } catch (_) {\n        return 0;\n    }\n    let pulled = 0;\n    const dlOpts = { maxAttempts: 2, retryDelayMs: 400 };\n    for (const ent of list) {\n        if (!ent || !ent.name || ent.type === FTP_FILE_TYPE_DIRECTORY) continue;\n        const name = ent.name;\n        if (!name.startsWith('animals_') || !name.endsWith('.json')) continue;\n        const remotePath = `${remoteDir}/${name}`;\n        const localFinal = path.join(localDir, name);\n        const tmpPath = `${localFinal}.tmp`;\n        let need = true;\n        if (await pathExists(localFinal)) {\n            try {\n                const localSize = (await fs.promises.stat(localFinal)).size;\n                const remSize = Number(ent.size);\n                if (Number.isFinite(remSize) && remSize > 0 && localSize === remSize) {\n                    const remMod = ent.modifiedAt instanceof Date ? ent.modifiedAt.getTime() : 0;\n                    const locMod = (await fs.promises.stat(localFinal)).mtimeMs;\n                    if (remMod && locMod >= remMod - 2000) {\n                        need = false;\n                    }\n                }\n            } catch (_) {\n                /* re-fetch */\n            }\n        }\n        if (!need) continue;\n        if (await safeDownload(client, remotePath, tmpPath, localFinal, dlOpts)) {\n            pulled += 1;\n        }\n    }\n    if (pulled > 0) {\n        console.log(`[FTP] [${srv.id}] Synced ${pulled} pen detail file(s) → ${localDir}`);\n    }\n    return pulled;\n}"};
function cjs(code,deps={},globals={}){const module={exports:{}};vm.runInNewContext(code,{module,exports:module.exports,require:n=>n in deps?deps[n]:require(n),console,URL,Buffer,Date,...globals});return module.exports;}
(async()=>{
const results=[];
const policy=cjs(sources.policy);
for(const u of ['file:///C:/Temp/index.html','file:///C:/Temp/setup.html','https://evil.invalid/','http://127.0.0.1:8766/']){
const actual=policy.isTrustedDashboardIpcUrl(u,{port:8768});results.push({case:'IPC trust '+u,actual,expected:false,pass:!actual});}
const imported=policy.sanitizeClassicConfigForRf({servers:[{id:'audit',mode:'ftp',ftpHost:'synthetic.invalid',ftpSecure:true,ftpRejectUnauthorized:true,httpFeedSecure:true,ftpPass:'FAKE'}]});
results.push({case:'import preserves secure transport policy',actual:{ftpSecure:imported.servers[0].ftpSecure??null,httpFeedSecure:imported.servers[0].httpFeedSecure??null,secretsRemoved:!imported.servers[0].ftpPass},expected:{ftpSecure:true,httpFeedSecure:true,secretsRemoved:true},pass:imported.servers[0].ftpSecure===true&&imported.servers[0].httpFeedSecure===true&&!imported.servers[0].ftpPass});
const config=cjs(sources.config),prev={servers:[{id:'audit',mode:'ftp',ftpHost:'synthetic.invalid',ftpPass:'FAKE'}]};
for(const field of ['ftpSecure','ftpRejectUnauthorized','httpFeedSecure']){const actual=config.configNeedsServerReboot(prev,{servers:[{...prev.servers[0],[field]:true}]});results.push({case:'transport-only save '+field,actual,expected:true,pass:actual});}
const guards=cjs(sources.origin+'\nmodule.exports={getRequestOrigin,enforceWriteOriginAndToken,webSocketOriginAllowed};',{},{
PORT:8768,isLocalServerHost:h=>['localhost','127.0.0.1'].includes(h),corsOriginAllowedPure:()=>false,MARKETING_SITE_HOSTS:[],getCachedLocalInterfaceIps:()=>[],
isLocalhostSocket:()=>true,isRequestFromThisMachine:()=>false,isLoopbackIp:()=>true,requestRemoteAddress:()=> '127.0.0.1'
});
for(const origin of ['null','::::','https://evil.invalid']){const req={headers:{origin,host:'127.0.0.1:8768'}};const write=guards.enforceWriteOriginAndToken(req,{}),ws=guards.webSocketOriginAllowed(req);results.push({case:'browser origin '+origin,normalized:guards.getRequestOrigin(req),writeAllowed:!write,wsAllowed:ws,expected:'reject write and websocket',pass:!!write&&!ws});}
const files=new Map([['final','LAST_GOOD']]);const fsMock={promises:{unlink:async p=>{files.delete(p)},stat:async p=>({size:(files.get(p)||'').length}),rename:async()=>{throw Error('EACCES injected')}}};
const dl=cjs(sources.download+'\nmodule.exports={safeDownload};',{fs:fsMock},{fs:fsMock,path,setTimeout});
const ok=await dl.safeDownload({downloadTo:async p=>{files.set(p,'NEW_BYTES')}},'remote','tmp','final',{maxAttempts:1});
results.push({case:'failed FTP replacement preserves last-good bytes',returned:ok,finalExists:files.has('final'),tmpExists:files.has('tmp'),expected:'false and final LAST_GOOD retained',pass:!ok&&files.get('final')==='LAST_GOOD'});
const captured=[];const root='C:\\audit\\userdata';
const sync=cjs(sources.ftp+'\nmodule.exports={syncFtpDetailsCache};',{}, {path:path.win32,fs:{promises:{mkdir:async()=>{}}},serverStates:{audit:{lastSaveSlot:'savegame1'}},FTP_FILE_TYPE_DIRECTORY:2,pathExists:async()=>false,safeDownload:async(_c,_r,_t,f)=>{captured.push(f);return true}});
await sync.syncFtpDetailsCache({list:async()=>[{name:'animals_x\\..\\..\\..\\..\\..\\..\\outside.json',size:4,type:1}]},{id:'audit'},'profile/savegame1',root);
results.push({case:'FTP detail listing stays inside userData',destinations:captured,expected:'rejected before download',pass:captured.length===0});
let slots=[],cursor=0,effects=[],dirty=false,currentOpts,output,calls=[],health=60;
const eq=(a,b)=>!!a&&a.length===b.length&&a.every((v,i)=>Object.is(v,b[i]));
const hooks={
 useMemo(fn,deps){const i=cursor++;if(!slots[i]||!eq(slots[i].deps,deps))slots[i]={value:fn(),deps};return slots[i].value},
 useRef(value){const i=cursor++;if(!slots[i])slots[i]={current:value};return slots[i]},
 useState(init){const i=cursor++;if(!slots[i])slots[i]={value:typeof init==='function'?init():init};return [slots[i].value,fn=>{const next=typeof fn==='function'?fn(slots[i].value):fn;if(!Object.is(next,slots[i].value)){slots[i].value=next;dirty=true}}]},
 useEffect(fn,deps){const i=cursor++;if(!slots[i]||!eq(slots[i].deps,deps)){const old=slots[i];slots[i]={deps,cleanup:old?.cleanup};effects.push(()=>{slots[i].cleanup?.();slots[i].cleanup=fn()})}}
};
const helper={
 collectPensNeedingDetailHydration:rows=>[...new Set(rows.filter(x=>x.synthetic).map(x=>String(x.husbandryId)))],
 mergeAnimalsWithPenHydration:(rows,by)=>rows.flatMap(x=>by[String(x.husbandryId)]||[x]),
 penDetailAnimalsLookIndividual:()=>true,
 penDetailAnimalsToLivestockRows:(_pid,list)=>list
};
const transpiled=ts.transpileModule(sources.hook,{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022}}).outputText;
const hook=cjs(transpiled,{'preact/hooks':hooks,'@/lib/pen-detail':{loadPenDetail:async(pid,opts)=>{calls.push({pid,serverId:opts.serverId});return {detail:{animals:[{husbandryId:pid,health,serverId:opts.serverId}]}}}},'@/lib/livestock-hydrate':helper,'@/lib/livestock-normalize':{normalizeLivestockAnimals:rows=>({animals:rows,lodState:{}})}});
function render(){cursor=0;dirty=false;output=hook.useHydratedLivestockAnimals(currentOpts);const now=effects;effects=[];now.forEach(f=>f());}
async function drain(){for(let i=0;i<8;i++){await new Promise(setImmediate);if(dirty)render()}}
currentOpts={animalsData:[{husbandryId:7,synthetic:true,health:0}],activeFarmId:1,activeServerId:'A'};
render();await drain();
results.push({case:'hydration initial mount control',calls:calls.length,health:output.animals[0]?.health,expected:60,pass:output.animals[0]?.health===60});
health=90;currentOpts={...currentOpts,animalsData:[{husbandryId:7,synthetic:true,health:0,generation:2}]};render();await drain();
results.push({case:'same-save newer generation refreshes hydration',calls:calls.length,health:output.animals[0]?.health,expected:90,pass:output.animals[0]?.health===90});
currentOpts={...currentOpts,activeServerId:'B',animalsData:[{husbandryId:7,synthetic:true,health:0}]};render();await drain();
results.push({case:'same pen id in new save fetches new detail',calls:calls.slice(),health:output.animals[0]?.health,expected:'B detail fetched, health 90',pass:calls.some(c=>c.serverId==='B')&&output.animals[0]?.health===90});
console.log(JSON.stringify({fixtureScope:'isolated mocks; no live requests, credentials or filesystem mutations',results},null,2));
})().catch(e=>{console.error(e);process.exitCode=1});

