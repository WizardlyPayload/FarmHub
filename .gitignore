# Dependencies
node_modules/
.venv/
venv/


# Electron / electron-builder output
release/
release-build/
release-*/
dist/
out/
electron-pack-out/
electron-pack-out-alt/

# Logs and local env
*.log
.env
.env.*

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.idea/
.vscode/
*.swp
*.swo

# Giants FS game data extract (huge; keep local only â€” do not sync to GitHub)
dataS_py_extracted/
dataS_py_extract/

# Mod shop images exported from FS25 mods folder (local only; not for git)
FS25_FarmDashboard_App/web/assests/img/items_mod_extract/*
!FS25_FarmDashboard_App/web/assests/img/items_mod_extract/.gitkeep

# Optional: keep built installers only in GitHub Releases, not in git
# (uncomment if you prefer never to commit installers)
# *.exe

# Marketing site (local / VPS ï¿½ not pushed to GitHub)
Website/

# Third-party FS25 mods (local reference / compat testing — do not push to GitHub)
FS25_*/
!FS25_FarmDashboard_App/
!FS25_FarmDashboard_Mod/

# Built mod zip — produce via tools/Zip-FarmDashboardMod.ps1
FS25_FarmDashboard_Mod/FS25_FarmDashboard.zip

# Built mod zip — produce via tools/Zip-FarmDashboardMod.ps1
FS25_FarmDashboard_Mod/FS25_FarmDashboard.zip

# Local agent secrets (VPS SSH — never commit)
.cursor/secrets/

# Unmapped / spare manual screenshots (timestamp grabs, superseded fd-*)
docs/screenshots/discard/
