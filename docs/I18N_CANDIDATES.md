# i18n candidate strings

Auto-generated by `web/locales/find-hardcoded-strings.mjs` (`npm run i18n:scan`). Each row is a literal that looks user-visible and is not already routed through `t()`. The suggested key is a slug hint — use a proper namespace when converting.

> **Status:** all 4 remaining hits below are confirmed false positives — the
> actual user-visible text inside each line is already a `t()` interpolation
> (`${emptyLivestockMsg}`, `${label}`) or a Bootstrap class string. The
> wrapper HTML/CSS is not a translation candidate. Re-run `npm run i18n:scan`
> after any new edits.

Total candidates: **4** across **3** files.

## FarmHub/FS25_FarmDashboard_App/FS25_FarmDashboard_App/web/assests/js/modules/apiStorage.js

| Line | Rule | Snippet | Suggested key |
| ---: | --- | --- | --- |
| 185 | innerHTML / outerHTML / textContent assign | `<span class="badge bg-danger">${label}</span>` | `apiStorage.span_class_badge_bg_danger` |

## FarmHub/FS25_FarmDashboard_App/FS25_FarmDashboard_App/web/assests/js/modules/changes.js

| Line | Rule | Snippet | Suggested key |
| ---: | --- | --- | --- |
| 349 | innerHTML / outerHTML / textContent assign | `<div class="text-center text-muted py-4"><i class="bi bi-info-circle me-1"></i>${emptyLivestockMsg}</div>` | `changes.div_class_text_center_text` |
| 352 | innerHTML / outerHTML / textContent assign | `<div class="text-center text-muted py-4"><i class="bi bi-info-circle me-1"></i>${emptyWarningsMsg}</div>` | `changes.div_class_text_center_text` |

## FarmHub/FS25_FarmDashboard_App/FS25_FarmDashboard_App/web/assests/js/modules/navigation.js

| Line | Rule | Snippet | Suggested key |
| ---: | --- | --- | --- |
| 540 | template literal (no placeholders) | `list-group-item list-group-item-action bg-secondary text-light d-flex justify-content-between align-items-center` | `navigation.list_group_item_list_group` |
