---
description: After editing FS25 FarmDashboard Lua under FS25_FarmDashboard_Mod, repack the mod zip for deploy/tests.
globs: FarmHub/FS25_FarmDashboard_Mod/**/*.lua
---

# FS25 Farm Dashboard mod — repack after Lua changes

The game loads **`FS25_FarmDashboard.zip`** (or an extracted folder) under **`mods/`**. Edits under `FS25_FarmDashboard_Mod/FS25_FarmDashboard_Mod/src/` are **not** picked up until you rebuild the archive (or copy files to the server’s mod tree).

From repo root **`FarmHub`** (parent of `FS25_FarmDashboard_Mod` and `tools`):

```powershell
.\tools\Zip-FarmDashboardMod.ps1
```

Output: **`FS25_FarmDashboard_Mod/FS25_FarmDashboard.zip`** with **only** **`modDesc.xml`**, optional **`icon.png`**, and **`src/`** at the **archive root** (forward-slash paths inside the zip — no extra files). Place the zip in **`mods\`**, or extract into **`mods\FS25_FarmDashboard\`** so **`modDesc.xml`** sits next to **`src`**. Upload/replace on dedicated host and restart if needed.
